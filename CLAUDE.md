# CLAUDE.md

## Инфраструктура: какой хост актуальный

**По умолчанию работай на этой машине.** Исключение — старый прод, см. ниже.

| | адрес | статус |
|---|---|---|
| текущий prod | `31.128.43.113` (hostname `nazicimzxh`) | **живой, это здесь** |
| бывший prod | `45.130.212.62` (hostname `oladaymbzh`) | **временно поднят, ssh работает** |

Эта машина изначально была dev-сервером и в июле 2026 стала продом.

**29 июля 2026 старый прод восстановили** — вопреки тому, что здесь было
написано раньше. Он живой, крон бэкапов на нём отрабатывает, ssh с этой
машины пускает по ключу без пароля. Оттуда забраны дампы за 28 и 29 июля
(`/root/backup/directus_*.sql`, `n8n_*.sql`), лежат в
`/root/restore-old-prod/old-prod-dumps/` в gzip.

Поднят он **на ограниченное время**. Если с него ещё что-то нужно — забирать
сейчас, а не потом. Быстрее всего `rsync -z` (plain SQL жмётся ~17×):

```
rsync -avz --partial -e 'ssh -o BatchMode=yes' \
  root@45.130.212.62:/root/backup/ФАЙЛ /root/restore-old-prod/
```

Когда его снова отключат — эту таблицу поправить обратно.

Осторожно с ssh-алиасами: в конфиге пользователя алиас с именем `prod` в
разное время указывал на разные машины. Не полагайся на имя алиаса — сверяйся
с IP. На самой этой машине `~/.ssh/config` отсутствует, алиасов нет вообще.

### Устаревшие ссылки в репозитории

Упоминания `45.130.212.62` остались в исторических файлах — это archeology,
не инструкции к исполнению:

- `_archive/scripts/*` — миграционные скрипты переезда на эту машину
  (`migrate_directus_to_new_server.sh`, `auto_migrate_directus.sh`,
  `get_dumps_from_old_server.sh` и т.п.), все отработали и больше не нужны
- `docs/prompts/codex-prod-analytics-scraper-dedup-2026-07-20.md`

## Данные

БД `directus` — форк старого прода, снятый ~11 месяцев назад, в самом начале
разработки. С тех пор обе базы развивались независимо.

- Postgres: контейнер `root-postgres-1`, данные bind-mount `/root/postgres`
- Directus: контейнер `root-directus-1`

### Бэкапы

`/root/backup_pg.sh` по крону в 03:00, результат в `/root/backup`.

Прежняя версия скрипта звала `pg_dumpall` на хосте, где клиента Postgres нет,
и **с 27.01.2026 по 29.07.2026 каждую ночь молча писала файл на 0 байт**.
29 июля переписан: дамп идёт через `docker exec`, формат `-Fc`, отдельно
роли/права, ротация 7 дней. Добавлены проверки размера и чтения через
`pg_restore -l` — именно их отсутствие и позволило полугоду пустых файлов
пройти незамеченным. При правке скрипта эти проверки не выкидывать.

В S3 бэкапы **не заливаются**, см. «Известные проблемы».

### Зеркало старого прода: `/root/sync-old-prod.sh`

Старый прод `45.130.212.62` — **не архив, а живой второй прод**: обслуживает
`smm.omemo.tech` (DNS этого домена смотрит туда, а не сюда), там работают
тестировщики и заводят баги в Jira. Данные двух серверов расходятся в обе
стороны.

Скрипт зеркалит его БД сюда, односторонне, в отдельную базу **`old_prod_sync`**.
Живая `directus` не участвует и не может пострадать. Запуск по крону раз в два
часа, прогон ~110 с, снимок ~235 МБ.

Устройство: `pg_dump -Fc` выполняется в контейнере старого сервера, результат
идёт потоком через ssh — на старом проде не меняются ни крон, ни конфиг, ни
диск. Восстановление сперва во временную базу, проверка (≥40 таблиц), и только
потом замена прошлого зеркала. Оборванный дамп или отключение старого сервера
посреди прогона оставляют предыдущее зеркало целым. `DROP DATABASE` ограничен
префиксом `old_prod_sync*`.

Восемь последних снимков лежат в `/root/restore-old-prod/sync/` — это архив на
момент, когда старый сервер отключат насовсем.

Логическую репликацию Postgres не используем сознательно: она потребовала бы
`wal_level=logical` и перезапуска боевого Postgres на временно поднятой машине.

### Смена боевой базы 29.07.2026

**Боевая `directus` — это теперь данные старого прода**, восстановленные из его
дампа за 29.07.2026 02:00. Прежний форк этого сервера переименован в
**`directus_fork_backup`** и оставлен как есть. Переименование, не копирование:
обе базы целы, ничего не удалялось.

```
directus              2222 МБ  боевая (данные старого прода)
directus_fork_backup  1748 МБ  прежний форк этого сервера, отставлен
old_prod_sync         2224 МБ  зеркало старого прода, см. ниже
```

Управляется через `DIRECTUS_DB` в `/root/.env`.

**Что осталось только в `directus_fork_backup`** и пропало из продукта — учитывать
при слиянии на переносе домена:

- `leads_qualification` — 29 011 строк, таблицы в данных старого прода нет вовсе.
  Вся пачка создана одним днём 25.05.2026 и с тех пор не росла. Из них 1 536
  «тёплых» и 135 «горячих» лидов.
- `video_projects` — 75 проектов за 12.05–27.06. В боевой сейчас 13 за 22.06–04.07,
  то есть новее, но их меньше. Наборы разные, ни один не покрывает другой.
- `directus_webhooks` — таблица снесена миграцией `20251224A Remove Webhooks`,
  которая есть в данных старого прода и которой не было в форке.

Обе базы расходились в обе стороны, ни одна не была надмножеством другой.
Сравнение на момент переключения:

| | старый прод | текущий |
|---|---:|---:|
| `directus_users` | **96** | 69 |
| `directus_activity` | **3 109 434** | 3 000 830 |
| `directus_revisions` | **737 777** | 630 284 |
| `post_comment` | **33 589** | 23 518 |
| `leads_qualification` | *таблицы нет* | **29 011** |
| `video_projects` | 13 | **75** |

Схемы почти совпадают: 47 таблиц из 49 общие. Уникальны `payment_activations`
и `promo_reservations` (данные старого прода), `directus_webhooks` и
`leads_qualification` (форк). Обратное слияние — только выборочно по таблицам,
с разрешением конфликтов по id.

Снимок форка, снятый до всех манипуляций, когда он ещё был боевым:
`/root/restore-old-prod/SAFETY-current-20260729-0623.sql.gz`.

Не выполняй разрушающих операций с БД и её томом без явного подтверждения
владельца: `DROP`, `TRUNCATE`, миграции с потерей данных, `docker compose down
-v`, удаление или перезапись `/root/postgres`.

## Развёртывание

Compose-стек лежит **вне этого репозитория**: `/root/docker-compose.yml`
(compose-проект `root`, рабочая директория `/root`).

Контейнеры проекта: `smm` (приложение, 5000), `root-traefik-1` (публичные
80/443), `root-postgres-1`, `root-directus-1` (8055), `root-n8n-1`
(127.0.0.1:5678), `root-pgadmin-1`.

На хосте есть и посторонние проекты — `zhdanov-pw`, `video_app`,
`watchsound-app`, `shadowsocks-server`, `mtproto-proxy`. Их не трогай.

Traefik обслуживает живой публичный трафик: перезапуски стека роняют боевой
сайт. Согласовывай их.

## Известные проблемы

### Бакет Beget S3 отдаёт объекты анонимно

Бакет `6e679636ae90-ridiculous-seth` (эндпоинт `s3.ru1.storage.beget.cloud`,
регион `ru-central-1`, креды `BEGET_S3_*` в `/root/.env`) обслуживает медиа
приложения и **отдаёт любой объект без авторизации, по HTTP 200**. Листинг
закрыт (403), но это слабая защита — имена ключей угадываются.

Следствия:

- В корне бакета с 23.06.2025 **лежит** `.env` с боевыми секретами, включая
  `DIRECTUS_ADMIN_TOKEN`. Скачивается кем угодно больше года. Локальная копия
  на случай удаления:
  `/root/restore-old-prod/old-server-config/LEAKED-bucket-root.env` (600).
  Версионирование в бакете выключено — удаление объекта необратимо.
  Из 6963 объектов бакета чувствительный только этот.
- Утёкший `DIRECTUS_PASSWORD` совпадает с текущим `POSTGRES_PASSWORD`
  контейнера `root-postgres-1`.
- Смягчает: 5432 и 8055 наружу не опубликованы, только внутри docker-сети.
  Но Directus торчит в интернет через Traefik, а токен статический.

**Дампы БД в этот бакет заливать нельзя** ни под каким префиксом. Для
бэкапов нужен отдельный приватный бакет, дампы шифровать перед загрузкой.

Статус на 29.07.2026: ротация токена и паролей **не выполнена** — требует
перезапуска боевого стека, согласовывать с владельцем.

## Git

Remote: `git@github.com:signmark/smm-video.git`

28 июля 2026 `origin/main` был переписан force-push'ем на новую историю
(`6a053eab3`) — **у неё нет общего предка со старой**. Прежний `main`
(`4d892320`) сохранён локально в ветке `main-old-2026-05-25`, а также есть на
`origin/partner_codes`, `origin/security-hardening`,
`origin/trends-batch-callback`.

Практическое следствие: ~150 старых веток стоят на несвязанной истории и
**не мержатся в текущий `main`**. Переносить работу оттуда только через
`cherry-pick` или `format-patch`, не через `merge`.

## Jira

Проект ведётся в Jira Server/DC: `https://jira.nutrientplaner.ru`.

**Доступ.** Креды лежат на хосте в `/root/.config/jira/env` (права 600, в
репозиторий не коммитятся). Подключение:

```bash
set -a; . /root/.config/jira/env; set +a
curl -sS -H "Authorization: Bearer $JIRA_TOKEN" "$JIRA_URL/rest/api/2/myself"
```

Важно: это Jira **Server/DC**, а не Cloud. Авторизация только Personal Access
Token через `Authorization: Bearer`. Схема `Basic email:token` из Jira Cloud тут
не работает — не тратить на неё время.

**Проекты.**

| ключ | что | кто заводит |
|---|---|---|
| `SM` | smm manager — баги от тестировщиков | тестировщики |
| `AI` | «AI Маркетинг», мёртв с февраля 2025 (31 задача, все от 2025) | никто |

**Типы задач** в SM: `Задача`, `Подзадача`, `История`, `Ошибка`, `Epic`.
**Переходы** (id для `POST /rest/api/2/issue/{key}/transitions`):
`11` → Сделать, `21` → В работе, `31` → Готово, `41` → Протестировано.

**Права аккаунта `signmark`:** `CREATE_ISSUE`, `ADMINISTER_PROJECTS`,
`BULK_CHANGE` есть; глобального `ADMINISTER` нет — **создать новый проект
нельзя**. Отсюда открытый вопрос ниже.

**Открытый вопрос:** владелец хочет отдельный бэклог с префиксом `DEV-`, чтобы
инженерные задачи не мешались с багами тестировщиков. Создать проект некому:
глобального админа нет ни у владельца, ни у бота. Варианты: (1) кто-то с правами
админа заводит проект `DEV`; (2) переименовать мёртвый `AI` в `DEV` — это
поменяет ключи у всех существующих `AI-*`, поэтому требует явного разрешения
владельца; (3) вести в `SM` типом `Задача` под отдельным эпиком — префикс
останется `SM-`.

### Рабочий процесс с тикетами

- Баг закрывается только после выката на прод и проверки. В комментарии — что
  было, в чём причина, каким коммитом, как проверено.
- Не закрывать непроверенное. Если подтвердить не удалось — оставить открытым и
  написать, каких данных не хватает.
- «Работает по замыслу» — законный исход, но с объяснением и предложением
  завести отдельную задачу на изменение поведения.

## Состояние работ (на 29.07.2026)

### Сделано и на проде

- Явная авторизация на `/api` — `server/middleware/api-auth-gate.ts`. Раньше API
  закрывался побочным эффектом: шесть роутеров были смонтированы как
  `app.use('/api', ...)` с верхнеуровневым `router.use(authenticateUser)`. Все
  шесть расшиты на per-route. Публичные исключения — единым списком с причинами.
- Durable state machine промокодов и платежей: CAS-переходы брони через
  update-by-query Directus, отзыв платежа при несостоявшейся привязке,
  возобновляемое погашение промокода, поля `needs_reconciliation`/`last_error`
  в `promo_reservations`.
- Граница арендатора в публикующих ручках, конвертере видео, экспорте отчёта,
  трендах, `/api/campaigns/sources`. Удалены бэкдоры `/api/test-youtube-publish`
  и `/api/publish/direct-youtube`.
- Три цепочки RCE через ffmpeg/curl закрыты, единый слой `server/utils/media-exec.ts`.
- XSS через `proxy-image`, утечки секретов в логи и ответы.
- Клиентский тайпчек 102 → 17 ошибок, удалено 7 мёртвых сломанных файлов.
- SM-9 (время публикации по Москве), SM-10 (закладки трендов).

### Новые release blockers из ревью Codex 29.07.2026

Повторное ревью диапазона `c3f85c069~1..745fcf568` нашло три незакрытых P1:

1. `/api/publish/cancel/:contentId` может изменить чужой контент через
   cross-user fallback в `storage.getCampaignContentById` и последующий
   `storage.updateCampaignContent` без пользовательского токена.
2. `collect-comments`, `collect-comments-single` и `analyze-comments` проверяют
   переданный `campaignId`, но затем служебным токеном работают с произвольным
   `trendId`, не связанным с проверенной кампанией.
3. Бронь промокода с `needs_reconciliation`, оставленная после неоднозначной
   отмены платежа, через 30 минут всё равно попадает под orphan-reclaim, потому
   что `isSlotHolderDead` видит пустой `yookassa_payment_id`.

Также остались ложное `exhausted` на TOCTOU слота, фиксированный потолок 200
попыток безлимитного промокода и локальные границы дня в календаре/метриках
после SM-9. Полный разбор и сценарии:
`docs/reviews/codex-review-2026-07-29-release-blockers.md`. Исполнительский
handoff: `docs/prompts/claude-fix-codex-review-2026-07-29.md`.

### Известные незакрытые (приоритет сверху вниз)

1. **Публичные callbacks трендов пишут в любую кампанию без авторизации.**
   Проверено на проде: `POST /api/trends/collect-trends-callback`,
   `/api/trends/collect-comments-callback`, `/api/trends/tg-webhook` отвечают
   200 без сессии. Причина: `registerTrendsRoutes(app)` в `server/index.ts:355`,
   а гейт на 393. Чинить осторожно: их зовёт внешний скрейпер, чью конфигурацию
   мы не видели — нужен секрет и привязка к серверной задаче, а не заплатка.
2. Оставшиеся cross-tenant ручки трендов: `/api/trend-comments/:trendId`,
   `/api/trend-sentiment/:trendId`, `/api/sources/:sourceId/analyze`,
   `/api/trends/collect-direct`, весь блок scraper monitoring.
3. Страница видео зовёт несуществующий `/api/beget-s3/upload-video`; настоящий
   путь `/api/beget-s3-video/upload`, и поле ответа `videoUrl`, а не `url`.
4. Клиентские запросы без авторизации: `SocialMediaSettings.tsx`,
   `VideoStoryEditor.tsx` (EventSource), `SimpleStoryEditor.tsx`.
5. Токены соцсетей в query string (`/api/vk/groups?access_token=` и соседние).
6. `server/routes/video.ts` — декодированный `:filename` идёт в путь;
   непереносимые тесты `media-exec` с жёстким `/etc/passwd`.
7. Остальные 17 ошибок `npm run check:client`.
8. Мёртвые, но исправные компоненты: `Calendar`, `Navbar`,
   `components/analytics/*`, `InstagramSetupWizardComplete`.
9. Переезд на `smm.nplanner.ru` как единственный canonical origin
   (`APP_PUBLIC_URL`), оба домена в allowlist/CORS.

### Согласованный следующий шаг

CI (не CD) в таком порядке: дочистить 17 ошибок клиентского тайпчека → GitHub
Actions на пять шагов (`npm ci`, `check`, `check:client`, `vitest`, `build`) →
дымовой скрипт после выката → Playwright на 3 сквозных сценария.

CD сознательно **не** делаем: стенда нет, прод единственный; раннеры GitHub
снаружи, и чтобы деплоить, пришлось бы либо открывать SSH наружу, либо ставить
self-hosted runner на боевой машине — это выполнение кода из репозитория от рута
на проде. Деплой узким местом не является: две команды и три минуты.
