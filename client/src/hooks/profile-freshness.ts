/**
 * Свежесть профиля (AI-64).
 *
 * Тариф меняется ВНЕ приложения: владелец одобряет заявку в Telegram или по
 * ссылке из письма. Вкладка с панелью об этом не узнаёт никак, поэтому
 * единственный момент, когда можно обновиться, — возврат в неё.
 *
 * Было staleTime 5 минут при refetchOnWindowFocus: true — и это сочетание не
 * работает: пока данные считаются свежими, фокус запроса не вызывает. Человек
 * одобрял тариф и возвращался в панель, где всё те же старые значения.
 *
 * staleTime: 0 означает «при возврате в панель перечитать» — ровно то, что
 * нужно для внешнего изменения. Профиль лёгкий, лишний запрос дешевле, чем
 * показанный не тот тариф.
 */
export const PROFILE_FRESHNESS = {
  staleTime: 0,
  refetchOnWindowFocus: true,
  refetchOnMount: true,
} as const;

