/**
 * Константы бренда и адреса, видимые пользователю (AI-89).
 *
 * ЗАЧЕМ. Раньше тексты в `AutonomousSettings.tsx` и `SupportChat.tsx`
 * прямо содержали `https://smm.omemo.tech`, `support@omemo.tech` и т.п.
 * Для второй установки под свой домен (решение владельца от 22.08)
 * это означало бы правку исходников в нескольких местах и риск
 * рассинхрона. Здесь — единая точка: те же значения, что были
 * захардкожены, но как именованные константы.
 *
 * ЧТО МЕНЯТЬ ПРИ ПЕРЕЕЗДЕ.
 *   - `appUrl` — публичный адрес установки (для приглашений).
 *   - `productUrl` — короткое упоминание продукта в подписи.
 *   - `supportEmail` — email поддержки.
 *   - `supportTelegram` — Telegram-канал поддержки.
 *   - `legalPaths` — пути к оферте/политике (относительно корня).
 *
 * ЧЕГО НЕ ДЕЛАЕТ. Не принимает данные от `/api/config` — это будет
 * отдельной задачей (server-driven brand). Сейчас файл задаёт
 * дефолты для нашей установки и закрывает путь «захардкоженные
 * адреса в JSX» как класс проблем.
 */

/**
 * Публичный URL приложения. Используется в приглашениях
 * (`AutonomousSettings.tsx`, alwaysInclude/signature по умолчанию)
 * и в местах, где ссылка должна вести на ту же установку, в которой
 * сидит пользователь.
 */
export const BRAND = {
  appUrl: 'https://smm.omemo.tech',
  productUrl: 'https://omemo.tech',
  supportEmail: 'support@omemo.tech',
  supportTelegramHandle: 'omemo_support',
} as const;

/**
 * Пути к правовым документам. Используются в `register.tsx` для
 * ссылок на оферту и политику. Сейчас ведут на статический подкаталог,
 * который в нашей установке отдаётся nginx-ом (`/smmniap_static/`).
 *
 * Если во второй установке правовые документы лежат по другому пути —
 * правится здесь, а не в форме регистрации.
 */
export const LEGAL_PATHS = {
  terms: '/smmniap_static/terms.html',
  privacy: '/smmniap_static/privacy.html',
} as const;
