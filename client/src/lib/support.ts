/**
 * Контакты поддержки.
 *
 * ЗАЧЕМ. В шапке (#86 / AI-134) появился пункт «Написать в поддержку».
 * Этот же адрес уже используется в `pages/payment/success.tsx` (там он
 * ведёт на Telegram) и `pages/payment/cancel.tsx` (там на почту).
 *
 * Храним оба канала в одной константе, чтобы при смене контакта правка
 * была одна, а не поиск по проекту. Домен у нас в планах на переезд
 * (это общая практика, не наше дело), почта после этого может смениться,
 * а Telegram-канал — нет.
 */

export const SUPPORT = {
  /**
   * Telegram-канал поддержки. Используется в шапке (AI-134) и в
   * `pages/payment/success.tsx` — там кнопка «Написать в поддержку»
   * ведёт именно сюда.
   */
  telegram: 'https://t.me/omemo_support',
  /**
   * Email поддержки. Используется в `pages/payment/cancel.tsx`. Домен
   * может смениться при переезде — если такое случится, правим
   * одну строку здесь, а не два файла.
   */
  email: 'support@omemo.tech',
} as const;
