# Owner — профиль инженера (Dmitry Zhdanov)

**Снимок:** 2026-07-28. Ведёт: агент по поручению владельца.
**Охват:** 44 репозитория `signmark/*` — не только этот. Что просмотрено и что нет — в конце файла.
**Зачем этот файл:** до него в проекте была расписана каждая модель (`hermes.md`, `mavis.md`,
`claude.md`), но не человек, который ставит задачи. Здесь — то, что не выводится из `git log`
одного репозитория: чем владелец занимается инженерно и что готов брать на заказ.

**Кто:** Dmitry Zhdanov (`signmark`). **Технический владелец продукта · full-stack инженер
(Python, TypeScript)**, в разработке с 2002 года — формулировка из его резюме
(артефакт «Дмитрий Жданов — резюме», версия 3, получен 2026-07-28).
Второе самоопределение, из `hermes-vault/life-os/01_Identity_MOC.md`: «Senior Architect /
Fullstack Developer, ИИ как продолжение мысли и рук — архитектура сверху, рутина делегируется
агентам». Главный продукт — **Omemo.tech** (SMM Manager, прод https://smm.omemo.tech).

**Контакты, трудовая история, обучение — не здесь.** Личная часть профиля живёт в хранилище
владельца: `hermes-vault/memories/USER_PROFILE.md` (указатель на него есть и в
`memories/USER.md`). Этот файл — только инженерная часть: классы задач, стек, что берётся
на заказ, с якорями на код.

---

## Рабочая модель

Соло-инженер, ведущий несколько продуктов **составом ИИ-агентов**. Что из этого следует для
каждой сессии:

- **Решения и приоритеты — за ним.** Ротация кредов, включение флагов, спорные трейд-офы —
  owner-gate (см. «Push policy» в `AGENTS.md`).
- **Проверяет результат на живом проде**, а не по отчёту агента: ночные QA-прогоны со sweep'ом
  эндпоинтов, нагрузкой и разбором логов — `docs/qa/prod-qa-journal-2026-07-27.md`.
- **Не верит документу против наблюдения.** Урок v4 в `AGENTS.md` (день работы по
  несуществующему составу агентов) — его правка.
- **Держит процесс как инженерный артефакт.** Это не побочный эффект, а отдельная линия работы:
  `AGENTS.md` и профили агентов здесь, шина `zookeeper` для координации моделей, память и скиллы
  в `hermes-vault`.
- **Стиль:** русский, коротко, таблицы и чек-листы; ожидает действие вместо обсуждения действия
  (`hermes-vault/memories/USER.md`). Не терпит выдуманных фактов и «отчётов» о несделанной работе.

---

## Опыт до текущих проектов

Репозитории показывают только последние полтора года. До них — два с половиной года
коммерческой разработки на Python и JavaScript в аутсорсе (заказчики из США и с Ближнего
Востока, команды по 5–10 человек, прямой контакт с заказчиком) и до того полтора десятка лет
веб-разработки и руководства небольшими командами на заказных проектах. Компетенции оттуда
разнесены по классам задач ниже — в первую очередь §17 (блокчейн, PHP) и §18 (заказная модель).

**Работодатели, даты, обучение, сертификаты и контакты — в
`hermes-vault/memories/USER_PROFILE.md`.** Здесь их сознательно нет: продуктовый репозиторий
не место для персональных данных.

---

## Классы инженерных задач

Каждый пункт подтверждён кодом или документом; в скобках — репозиторий-источник.

### 1. Интеграции соцсетей и публикующий конвейер `[smm-video, smm-niap]`
Telegram, VK, Instagram, Facebook, YouTube, Threads (TikTok выключен в публикации).
OAuth-колбэки на каждую сеть, обновление протухающих токенов (`vk-token-refresh.ts`,
`youtube-token-refresh.ts`), планировщик с защитой от дублей (`publish-scheduler.ts`,
`publication-lock-manager.ts`), единый сервисный токен публикации (`publishing-token.ts`),
санитайзер OAuth-секретов (`.agents/memory/oauth-sanitizer-contract.md`), проверка владения в
публикующих ручках, форматные ограничения платформ (`instagram-video-validator.ts`).
`smm-niap` — предыдущее поколение того же продукта: два контура Directus (dev/prod), Beget S3,
мульти-AI. То есть продукт пройден дважды, со сменой архитектуры.

### 2. Генерация видео под ключ `[video-app (ReelForge), smm-video/video-app]`
Отдельный продукт: текст → готовый вертикальный ролик 1080×1920 с озвучкой, визуалом и вшитыми
субтитрами. Конвейер Qwen → FLUX (fal.ai) / Pexels → Edge TTS → FFmpeg; заявленный тайминг —
20-секундный ролик за 35–45 секунд. Во встроенной версии внутри `smm-video`: `director.ts`,
`script-generator.ts`, `fal-animator.ts`, `veo-generator.ts`, `stock-video.ts`,
`music-generator.ts`, `video-assembler.ts`, `resume-policy.ts`. Решённые нетривиальные места
зафиксированы в памяти: липсинк аватара под тот же TTS без двойного звука, длина музыки от
реальной длительности клипа, ретайминг клипа под аудио вместо `atempo`, монотонный прогресс,
трёхслойный промптинг, обязательный `stockPrechecked` перед `script_ready`.

### 3. Инфраструктура мультиагентных систем `[zookeeper]`
Не «работа с ИИ», а сервер: HTTP-шина координации моделей на Fastify 5 + better-sqlite3.
Контракты возможностей агента (`accepts`/`produces`/`max_concurrent`/`heartbeat_ttl`), контуры —
именованные пайплайны стадий, очередь задач `pending → claimed → done/failed`, heartbeat с
requeue по истечении TTL (reaper «Zoo Watcher»), CLI-воркер, **handoff** — структурированная
передача контекста между стадиями, включая главный сценарий «модель упёрлась в лимит и осознанно
передала незаконченную работу дальше». Отдельно проработана безопасность: токен-аутентификация с
осознанными исключениями для kill-recovery, джейл артефактов по realpath, secret-scan payload'ов,
роль-гейт на задачи с доступом к файловой системе. Деплой — pm2, сеть — Tailscale-only.
Тесты (77) написаны как onboarding-протокол для новой модели.

### 4. Долговременная память и знаниевые пайплайны для ИИ `[hermes-vault]`
Хранилище автономного ассистента: трёхслойная архитектура raw → wiki → schema, правила
ингеста источника (одно добавление обновляет 5–10 файлов с перекрёстными `[[ссылками]]`),
каталог из ~40 скиллов (devops, research, content-pipeline, telegram_scout, red-teaming и др.),
крон, авто-бэкап с git-guard. Живой пайплайн: **AI Scout** — парсинг 46 Telegram-каналов →
фильтрация → структурированные дайджесты в хранилище.

### 5. Генеративный AI в продукте, мультипровайдерно `[smm-video, smm-niap, ai-business-team]`
Anthropic, Gemini (в т.ч. через собственный прокси — прямой вызов триального ключа даёт 403),
OpenAI, DeepSeek, Qwen, FAL (Flux, Nano Banana, Juggernaut), Vertex AI, Xiaomi MiMo.
Ассистент с маршрутизатором команд и порогом уверенности, голосовой ввод (Telegram-голосовые →
STT; Web Speech API в кабинете), русские ударения омографов через U+0301, учёт расхода генераций,
хранение и ротация ключей — глобальных и пользовательских.

### 6. AI-агенты как продукт `[ai-business-team, ai_daily_coach, concierge*, mysticoracle, neurosolution]`
`ai-business-team`: команда агентов, ведущая проект как агентство — PM разбирает бриф и раздаёт
задачи специалистам, у каждого своя личность и стиль речи, обсуждения двусторонне зеркалятся
между админкой и общим Telegram-чатом (Gemini + Express 5 + Drizzle + PostgreSQL).
`ai_daily_coach`: FastAPI + SQLAlchemy на бэке, React/pnpm на фронте, Directus + PostgreSQL,
Stripe, Telegram Mini App, Google OAuth. `concierge` (три итерации), `mysticoracle`,
`neurosolution` — быстрые AI-MVP на Gemini + Supabase, собранные из AI Studio-скаффолда за дни.

### 7. Аналитика соцсетей `[smm-video]`
Сбор и агрегация метрик, фоновое обновление, «лучшие часы» по собственным данным, вотчер
всплесков охвата и новых комментариев с доставкой в бот (`engagement-watcher.ts`,
`notify-user.ts`), отчёты (pdfkit/xlsx), дашборды на nivo/recharts.

### 8. Сбор данных и скрейпинг `[smm-video, hermes-vault, polymarket_trading_dashboad]`
Парсинг tgstat (в т.ч. Puppeteer под куками), Apify, XMLRiver, собственный веб-краулер и
AI-фильтр релевантности, HTTP/SOCKS-прокси, внешние скрейпер-API по ключу из Directus, сбор
трендов по расписанию. В `hermes-vault` — конкурентная разведка и сбор лидов (парсинг страниц
эффективности рекламы для сбора платёжеспособных рекламодателей). В polymarket-дашборде —
Playwright как источник данных.

### 9. Бэкенд на Directus + PostgreSQL `[smm-video, smm-niap, np-promo]`
Схема и её дрейф между dev и прод (Directus молча дропает несуществующие поля), политика токенов
(UI — только пользовательским, admin — только серверные задачи), прокси и CRUD-слой,
storage-адаптер и S3 (Beget + AWS SDK, presigned URL, Uppy на фронте), авторизация через Directus
в соседних продуктах.

### 10. Монорепо-шаблон и кодогенерация API `[video-app, ai-business-team, simple-wheels]`
Один и тот же «золотой шаблон» переиспользуется между продуктами: pnpm workspaces, Node 24,
Express 5, PostgreSQL + Drizzle ORM, Zod (`drizzle-zod`), **OpenAPI → Orval → сгенерированные
React Query-хуки и Zod-схемы** (`lib/api-spec`, `lib/api-client-react`, `lib/api-zod`, `lib/db`),
сборка esbuild, готовые Dockerfile и compose. Смысл шаблона — агенту не нужно писать ручные
фетчи и типы, они синкаются из спеки (`hermes-vault/SAAS_FACTORY_STRATEGY.md`).

### 11. Безопасность и QA на живом проде `[smm-video, zookeeper]`
Закрытая утечка публичного admin-токена, проверки владения на write-путях, fail-closed-гейты,
брутфорс-защита логина (проверено: 20×401 → 429), доверие тела вебхука YooKassa, WS-утечка,
заголовки безопасности и осознанный отказ от CSP/X-Frame-Options ради Telegram Mini App,
неотзываемый 24-часовой access-токен, ротация утёкших кредов как owner-решение. Спеки —
`docs/specs/spec-06…spec-15`, живой прогон — `docs/qa/prod-qa-journal-2026-07-27.md` (sweep 186
запросов по 93 путям, нагрузка ступенями 1→100, каждая находка разобрана до строки кода).
В `zookeeper` — отдельный документ модели угроз с явным перечнем того, что НЕ покрыто.

### 12. Инфраструктура и деплой `[smm-video, zookeeper, yanafamily, django_react*, zhdanov.pw]`
Docker + compose, окружение только через `env_file`, пересборка образа и выкатка, health-проверки,
проверка что новый код реально уехал (греп по бандлу ASCII-маркерами — esbuild экранирует
кириллицу), логирование и его объём, лимит памяти для vite build. Шире репозитория:
nginx/Traefik, gunicorn, certbot с авто-обновлением сертификата, pm2, Tailscale, VPS под SSH.

### 13. Биллинг и подписки `[smm-video, ai_daily_coach]`
YooKassa (вебхуки, планы, лимиты, партнёрские постбэки), Stripe в coach'е, глобальный гейт
истёкшей подписки (GET свободны, мутации 403), единый источник истины для баннеров
trial/paid/expired.

### 14. Telegram-боты и Mini App `[smm-video, ai-business-team, yanafamily, telegram_assistant]`
Telegraf-бот с голосовыми и уведомлениями, шина уведомлений, long-polling-мост «чат ↔ админка»,
Telegram Mini App и его ограничения, Python-бот на Django-стороне.

### 15. Фронтенд `[smm-video, np-promo, bike-rental семейство, soundvisualizer]`
React 18/19 + TanStack Query + Vite + Tailwind/shadcn/Radix, i18n, TipTap, Uppy. Отдельно —
производительность: сайдбар и дашборд, тянувшие по 5 МБ на каждой странице ради счётчиков,
переведены на `?summary=1`. В `soundvisualizer` — аудиовизуализация на p5.js + shaka-player.

### 16. Тесты и инженерные гейты `[smm-video, zookeeper]`
Vitest (1016 тестов / 94 файла зелёными на 2026-07-26), Playwright-смоуки, обязательная «красная
проверка» — новый тест обязан падать без фикса, и это проверяется снятием правки, а не
рассуждением; три обязательных гейта перед пушем (`vitest`, `tsc`, `build`); в `zookeeper` —
77 тестов по реальному HTTP.

### 17. Прочие домены, подтверждённые кодом
- **SEO и семантика** `[np-promo]` — платформа управления keyword-кампаниями: WordStat API,
  оценка потенциала роста, предсказание трендов.
- **Prediction markets** `[polymarket_trading_dashboad]` — дашборд на `@polymarket/clob-client`
  + ethers + SQLite; по заметке в памяти торговля прибыльная, но поставлена на паузу до
  автоматизации пайплайна.
- **P2P-аренда** `[bikerentalsaas, asia_bike_rental, rentmybikereplit, bike-rental-manager,
  simple-wheels]` — пять итераций одного продукта за 2025-12…2026-03, от Replit-прототипа до
  монорепо с генерируемым API-клиентом.
- **Python/Django** `[yanafamily, django_react, myawesomeblog, sha256_hash]` — Django + gunicorn +
  nginx + certbot в проде (2024), связка Django/React/docker/nginx (2022).
- **PHP** `[symfony, hexlet-php, hexlet-phpunit, php-project-lvl1]` — Symfony, PHPUnit, PHPStan,
  Makefile-обвязка (2021–2022) плюс 14 лет коммерческой работы на PHP до переквалификации.
- **Блокчейн / Web3** `[резюме, кода в публичных репозиториях нет]` — мультикошельковая
  инфраструктура на Django + Celery + Redis: балансы, правила агрегации средств, приватные ключи
  и подпись транзакций.

### 18. Заказная модель работы `[резюме]`
Отдельная компетенция, которой не видно в коде: два с половиной года в аутсорсе с прямым
контактом с заказчиком — сбор требований, демонстрация результата, согласование решений на
созвонах, работа в командах по 5–10 человек. Плюс более ранний опыт ведения проектов целиком:
ТЗ, набор и руководство командой, приёмка. То есть заказ он может брать не только как «руки»,
но и с постановкой задачи и сдачей.

---

## Что готов брать на заказ

Собственное позиционирование (`hermes-vault/plans/client-acquisition-week.md`, 2026-07-14):
**«AI Agent Developer | Telegram Bots | SaaS Automation»**, ставка в плане — **$30–50/час**,
площадки — Jobbers.io, Freelancer.com, Upwork, LinkedIn; портфолио — Telegram-бот с AI,
SaaS-автоматизация, парсеры/скрейперы, веб-приложения на Next.js / FastAPI.

Развёрнуто по классам задач выше:

1. **Автопостинг и интеграции соцсетей под ключ** — TG/VK/IG/FB/YouTube/Threads, OAuth, рефреш
   токенов, планировщик без дублей, форматные ограничения платформ.
2. **Генерация видео конвейером** — от текста до готового вертикального ролика: сценарий,
   изображения, анимация, озвучка с корректным русским произношением, музыка, субтитры, сборка.
3. **AI-агенты и ассистенты** — от одиночного бота до команды агентов с ролями и мостом в
   Telegram; распознавание команд, голосовой ввод, мультипровайдерная развязка, лимиты расхода.
4. **Мультиагентная инфраструктура** — шина задач, контуры-пайплайны, heartbeat и requeue,
   передача контекста между моделями, чтобы работа переживала лимиты любой из них.
5. **Память и знаниевые пайплайны** — сбор источников, структурирование, дайджесты, скиллы,
   поддержание базы в актуальном виде.
6. **Сбор данных** — скрейперы, краулеры, парсинг площадок под куками, прокси, AI-фильтрация.
7. **Аналитика и отчётность по соцсетям** — метрики, агрегация, «лучшее время публикации»,
   уведомления о всплесках, экспорт отчётов.
8. **Аудит безопасности и QA веб-приложения на живом стенде** — sweep эндпоинтов на авторизацию,
   межарендные утечки, нагрузочные ступени, разбор логов, отчёт с привязкой к строкам кода.
9. **Инфраструктура и деплой** — Docker/compose, nginx/Traefik, SSL, health-checks, логи и
   ротация, pm2, приватные сети.
10. **Биллинг и подписки** — YooKassa, Stripe, тарифы, лимиты, гейты доступа, постбэки.
11. **Быстрый MVP** — рабочее веб-приложение с AI внутри за дни, на обкатанном монорепо-шаблоне
    с генерацией API-клиента из OpenAPI.
12. **Постановка работы с ИИ-агентами в чужой команде** — роли, гейты, память проекта, шаблоны
    handoff'ов, правила ревью и деплоя.
13. **Автотесты под ключ** — покрытие Playwright/Vitest/pytest уже работающей системы,
    проектирование сценариев и стабилизация на большом объёме функциональности; отдельный опыт —
    крупная платформа мониторинга происшествий для заказчика с Ближнего Востока.
14. **Разбор и поддержка чужого легаси** — диагностика и починка унаследованной системы без
    документации, в проде; в своём продукте свёл семь параллельных клиентов одного AI-провайдера
    и шесть вариантов интеграции с другим к единым модулям.
15. **CI/CD и мониторинг** — пайплайны на GitHub Actions и Jenkins, Sentry на проде.
16. **Чат-боты и no-code-автоматизация бизнес-процессов** — salebot, n8n: интеграции с внешними
    API, обработка заявок, маршрутизация сообщений.
17. **Блокчейн-инфраструктура** — управление мультикошельковой системой: балансы, правила
    агрегации средств, приватные ключи и подпись транзакций (Django + Celery + Redis).

## Стек

**Языки:** Python и TypeScript/JavaScript (оба заявлены как основные), SQL; PHP — 14 лет
коммерческой работы до 2016-го, в текущих проектах не используется.
**Node:** Express 5, Fastify 5, Drizzle ORM, Zod, Orval/OpenAPI-codegen, WebSocket, Telegraf,
esbuild, pnpm-workspaces, tsx.
**Python:** Django, FastAPI, SQLAlchemy, Celery, Redis, gunicorn.
**Фронт:** React 18/19, TanStack Query, Zustand, Vite, Tailwind, shadcn/Radix, TipTap, p5.js,
Telegram Mini Apps.
**Данные:** PostgreSQL, MySQL, SQLite/better-sqlite3, Directus (auth, RBAC), Supabase,
S3 (Beget/AWS SDK).
**Тесты:** Vitest, Jest, Playwright, supertest, pytest.
**Инфра:** Docker/compose, nginx, Traefik, certbot, pm2, Tailscale, VPS/Linux, Replit;
CI/CD — GitHub Actions, Jenkins; мониторинг — Sentry.
**AI:** Anthropic, OpenAI, Gemini/Vertex, DeepSeek, Perplexity, Qwen, Xiaomi MiMo, FAL, Apify,
HeyGen, Edge-TTS, Whisper/STT — с мультипровайдерной оркестрацией и цепочками фолбэков;
Claude Code, Codex и локальные модели как исполнители.
**Интеграции:** VK API, Telegram Bot API, Meta Graph API, YouTube Data API, OAuth 2.0, Web3.
**Медиа:** FFmpeg/ffprobe, sharp.
**Исторически:** salebot и n8n (боты и no-code-автоматизации, 2024–2025), Drupal, Apache.

## Сверка резюме с кодом (2026-07-28)

Проверял утверждения резюме по этому репозиторию. Три подтвердились точно, два требуют правки.

| Утверждение | Проверка | Итог |
|---|---|---|
| «89 сервисов» | `ls server/services` → ровно 89 | ✅ |
| «40+ групп API-роутов» | `server/routes/*` → 58 плюс 9 `routes-*.ts` = 67 | ✅ (даже занижено) |
| «433 коммита за последние 2,5 месяца» | после `git fetch --unshallow`: 742 коммита всего, из них **433 у Dmitry Zhdanov** (остальные — Replit Agent 190, smmniap 52, Mavis 27, Hermes 27, Claude 8, signmark 4, Codex 1) | ✅ ровно |
| «682 тестовых файла» | `*.test.*`/`*.spec.*` → 152; файлов в тест-каталогах → 278; всё, где в имени есть `test` → 448; самый широкий подсчёт (объединение) → 495. Ни один способ не даёт 682. `AGENTS.md` фиксирует другой замер: 1016 тестов в 94 файлах, прогон зелёный | ⚠️ не воспроизводится |
| «n8n-воркфлоу для публикаций» и n8n в инфраструктуре | В коде n8n остался только текстами ошибок: `[social-publishing] n8n удалён: публикация через webhook недоступна`. Память проекта (`.agents/memory/n8n-removal.md`) — «n8n полностью удалён, все вебхуки заменены прямыми вызовами». Каталог `n8n-workflows/*.json` — артефакт прошлого состояния | ⚠️ описывает прошлое как настоящее |

Рекомендация по этим двум пунктам — решение владельца:
- вместо «682 тестовых файла» взять проверяемое «1016 тестов в 94 файлах, прогон зелёный» плюс
  описание харнесса — это и сильнее, и выдержит проверку;
- n8n перенести в прошедшее время (в разделе фриланса 2024–2025 он на месте, а в описании
  текущей инфраструктуры SMM Manager — уже нет).

## Открытое (решает владелец, агент сам не заполняет)

- Две цифры выше: подтвердить или поправить.
- Ставка. В резюме её нет; в плане поиска клиентов от 2026-07-14 — $30–50/час.
- Что из приватных репозиториев допустимо показывать в портфолио.
- Вопрос канонического адреса для внешних текстов — в `hermes-vault/memories/USER_PROFILE.md`,
  вместе с самими адресами.

## Охват источников

**Просмотрено:** 44 репозитория `signmark/*`, склонированных 2026-07-28 (`--depth 1`), плюс
**резюме владельца** (версия 3, получено 2026-07-28). Резюме закрывает 2002–2024, которых в
репозиториях нет вообще — блокчейн-проект, аутсорсные автотесты, разбор легаси, заказная
разработка и руководство командами. Персональная часть резюме сюда не переносилась, она в
`hermes-vault/memories/USER_PROFILE.md`.
**Пустые репозитории** (клон без файлов, в профиль ничего не дали): `codexskills`, `herm`,
`niap_ui`, `Smetomatika`, `Cost_Account`, `test`.
**Не подключались:** форки (`openclawd`, `example-voting-app`, `hexletguides.github.io`,
`getting-started-course-v1-app`, `docker-react-django-nginx`) — чужой код; и четыре
дубля bike-rental (`RentMyBike`, `RentMeBike`, `Rent_My_Bike_Replit`, `BikeRental`) — семейство
покрыто пятью другими репозиториями.
**Не в GitHub:** OmniRoute, Clipos Studio и инстансы Hermes упоминаются в `hermes-vault`, но
отдельных репозиториев у них нет — в профиль вошли только по описаниям, без кода.

## Как обновлять

- Новый класс задач попадает сюда **после того, как есть код или документ**, на который можно
  сослаться. Без якоря — не добавлять.
- Меняется рабочая модель (состав агентов, кто деплоит, где owner-gate) — правится здесь и в
  `AGENTS.md` в тот же день.
- Профиль охватывает все репозитории владельца, а лежит в `smm-video`. Второй экземпляр —
  `hermes-vault/memories/USER.md`; при расхождении источник истины — этот файл, `USER.md`
  подтягивать за ним.
