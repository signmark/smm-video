# Handoff: <агент> — <задача> — <дата>

<!-- Файл: docs/prompts/<агент>-<задача>-<дата>.md. Все секции обязательны; «нет» — валидный ответ, пропуск секции — нет. -->

## Задача

Одним абзацем: что делалось и почему. Ссылка на источник (пункт плана / баг / поручение owner'а).

## Коммиты

| Hash | Сообщение | Файлы |
|---|---|---|
| `xxxxxxx` | fix: ... | server/api/... |

## Что сделано

По пунктам, факты, без оценок «стало лучше». Что именно изменилось в поведении системы.

## Верификация (мой прогон)

- `npx vitest run`: X/X файлов, Y/Y тестов, длительность
- `npx tsc -p tsconfig.critical.json`: exit N
- Фронт-билд (если менялся client/): проходит / не менялся
- Новые регрессионные тесты: <файлы>; падают без фикса: да/нет (проверено как)

## Компромиссы и отклонения

Всё, что сделано не по канону или неочевидно: почему так, какие альтернативы отброшены. Отклонение от канонического цикла — обязательно здесь.

## Известные ограничения / не сделано

Что осталось за скоупом, что отложено, какие follow-ups созданы.

## Вопросы к ревьюеру / owner'у

Конкретные вопросы, требующие решения. Нет вопросов — писать «нет».

## Следующий шаг

Кто и что делает дальше (ревью Mavis / gate owner'а / деплой Mimo).
