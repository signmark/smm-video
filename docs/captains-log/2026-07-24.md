# Captain's Log — 2026-07-24

*(записано Claude по поручению owner'а: «прокачай агентов, чтобы разбирались не хуже тебя». Зона captain's log принадлежит Mavis — это разовое исключение по прямому поручению, single-writer не нарушен: Mavis в этот момент не писал.)*

## Контекст дня

День большой смены состава и передачи знаний. Owner потерял доступ к Codex, Claude и Kimi (лимиты). Остались: **Hermes** (разные модели), **Mavis** (MiniMax) и **Mimo**. Claude на остатках лимитов готовит проект к автономной работе тройки.

## Что произошло

1. **Roster-пакет** (`docs/prompts/claude-roster-handoff-2026-07-24.md`): AGENTS.md v2, профили `docs/agents/{hermes,mavis,mimo}.md`, шаблоны handoff/verdict/session-checklist. Роли: Hermes = исполнитель, Mavis = единственный ревьюер + docs/контекст, Mimo = deploy + infra-ревью.
2. **Security-прогресс:** §2 (admin-only scheduler, `e102578d`), §4 upload hardening + §5-low (`34a8ebf4`) закрыты; handoff `claude-security-2-4-5-2026-07-24.md`. §1 был закрыт ранее (`1473f4bf`). §3 DEFERRED до августа. Спеки §6-§15 написаны в `docs/specs/` (`f217c01c`) — исполнителю декомпозировать не нужно, спека = scope.
3. **Соцсети без токенов в браузере** (`15b649d8`, `05eb8aef`, deploy на prod ~16:02 UTC) + Facebook через IG-токен (`89be723f`) + VK блок «Группа для публикации» (`33b258c6`). Handoff: `hermes-social-ui-token-removal-2026-07-24.md`. Контракт sanitizer'а зафиксирован.
4. **Пакет передачи знаний** (эта запись — его часть):
   - `docs/agents/codebase-map.md` — карта кодовой базы: топология, порядок middleware в index.ts, три слоя роутов, сервисы, auth, поток публикации, метод диагностики.
   - `.agents/memory/oauth-sanitizer-contract.md` — контракт «фронт не читает токены» как урок (ломали дважды).
   - `docs/context/state.json` обновлён: новый roster, приоритеты (§7 CI следующий), open tasks OT-003..OT-005 из social-token долгов.

## Что пошло не так / риски

- Social-token коммиты ушли в prod **без новых регрессионных тестов** (осознанно, owner просил быстро) — долг OT-003, первый кандидат для Hermes после §7.
- Токены VK/IG/YouTube/Threads и appSecret'ы засветились во внешнем чате — ротация в августе вместе с §3 (OT-005), owner в курсе.
- Модели тройки Claude'у неизвестны — профили и процессы написаны model-agnostic; если Hermes окажется слабее Mavis, роли исполнитель/ревьюер зеркалятся правкой двух строк в AGENTS.md.

## Задача (следующий шаг)

- **Hermes:** входной ритуал по `hermes.md` (теперь включает `codebase-map.md` через AGENTS.md) → §7 CI по спеке → потом OT-003 (регрессионные тесты fallback'ов).
- **Mavis:** принять зону captain's log обратно; ревью следующего handoff'а Hermes по протоколу; поддерживать state.json.
- **Mimo:** деплой-цикл без изменений; следить за §11 (Docker) — там ты второй ревьюер.

## Критерий

Любой из тройки, открыв проект с нуля, за один входной ритуал (AGENTS.md → свой профиль → MEMORY.md → backlog → git log) понимает: что за система, где что лежит, что делать следующим и чего не трогать.

## Постскриптум (вечер, v3)

Owner вернул Claude в активный состав (AGENTS.md v3, профиль `docs/agents/claude.md`): роль — опциональный архитектурный ревьюер / автор плотных планов / co-author, HIGH priority при наличии оплаты Pro/Max. Штатный ревьюер кода — по-прежнему Mavis; тройка обязана оставаться полностью работоспособной и без Claude. `state.json.models` обновлён соответственно.

## Связи

- **Handoff'ы дня:** claude-roster-handoff, claude-security-2-4-5, hermes-social-ui-token-removal (все 2026-07-24, в `docs/prompts/`)
- **Memory:** + oauth-sanitizer-contract.md
- **Владелец:** Dmitry (поручение 2026-07-24: «прокачать агентов, использовать скилы/инструкции/фолапы/хендоффы»)
