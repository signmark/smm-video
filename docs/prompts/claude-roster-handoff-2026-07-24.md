# Handoff: Claude — смена состава агентов — 2026-07-24

## Задача

Поручение owner'а (Dmitry, 2026-07-24): в доступе остались только Гермес (Hermes), Мавис (Mavis/MiniMax) и Мимо (Mimo) на разных моделях; Codex, Claude и Kimi выбыли. Подготовить проект так, чтобы тройка закрывала все роли на прежнем уровне качества, полностью автономно (Claude из цикла выходит).

## Решения (утверждены owner'ом в диалоге)

1. **Распределение ролей — по силе/оснащённости:** Hermes (доступ к папке проекта + SSH к prod, инструменты уровня CLI-агента) → исполнитель, ex-зона Codex. Mavis → единственный штатный ревьюер (ex-зона Claude/Kimi) + прежняя docs-зона. Mimo → deploy + infra-ревью.
2. **Запрет «Mavis не пишет прод-код» снят условно** — только как fallback при недоступности Hermes и явном поручении owner'а; ревьюером в этом режиме становится Mimo. Правило «исполнитель ≠ ревьюер» ненарушимо.
3. **Слабости моделей компенсируются процессом:** обязательные входные/выходные ритуалы, Definition of Done, шаблоны с обязательными секциями, независимый прогон тестов на каждом этапе.
4. **SSH Hermes'а к prod** — read-only диагностика; деплой остаётся за Mimo (иначе теряется независимая пара глаз перед prod).

## Что сделано (файлы)

| Файл | Что это |
|---|---|
| `AGENTS.md` | v2: новый состав, адаптированный канонический цикл, таблица зон 2026-07-24, таблица преемственности выбывших |
| `docs/agents/README.md` | индекс + три железных правила |
| `docs/agents/hermes.md` | профиль исполнителя: ритуалы, границы, DoD, команды, грабли-дайджест, next-up (§2→§4→§5) |
| `docs/agents/mavis.md` | профиль ревьюера: протокол ревью (эталон — kimi-task8-review), fallback-режим, прежняя зона |
| `docs/agents/mimo.md` | профиль деплоера: pre/post-deploy чек-листы, rollback, infra-ревью |
| `docs/agents/templates/handoff-template.md` | шаблон handoff исполнителя |
| `docs/agents/templates/review-verdict-template.md` | шаблон вердикта ревьюера |
| `docs/agents/templates/session-checklist.md` | общий ритуал сессии + красные линии |

## Верификация

Docs-only изменения, код не тронут, тесты не требуются. Перекрёстные ссылки между файлами проверены. Чужой untracked WIP (`project_snapshot.txt`, `webbridge-req-kb*.json`, `zoo_analysis.md`, `PRIORITIZED_IMPROVEMENT_PLAN_2026-07-23.md`) не тронут.

## Компромиссы

- Модели Hermes/Mimo/Mavis точно не известны Claude — профили написаны model-agnostic, с расчётом на компенсацию процессом, а не на способности конкретной модели.
- Captain's log за 2026-07-24 не создан — это зона Mavis (single-writer); поручение ниже.

## Вопросы к owner'у

1. Подтвердить распределение: Hermes = исполнитель. Если у Hermes модель слабее, чем у Mavis, — роли исполнителя/ревьюера можно зеркально поменять, структура пакета это переживает (поменять две строки в таблице зон AGENTS.md).
2. Push policy оставлена как была (owner-gate, Mavis — docs-only). Менять?

## Следующий шаг

1. **Mavis:** прочитать свой профиль, записать captain's log за 2026-07-24 (смена состава), обновить `docs/context/state.json` (decisions + новый roster), закоммитить весь пакет (docs-only).
2. **Hermes:** входной ритуал по `hermes.md`, взять §2 (admin-only scheduler) по беклогу.
3. **Mimo:** прочитать свой профиль; деплой-цикл без изменений.
