# Финальное ревью цикла — хендофф для Claude (2026-07-20, Kimi)

От владельца: **нужна твоя зелёная отмашка — «всё готово к мержу на
проде»**. Пуш после твоего вердикта делает Mavis (делегировано
владельцем 2026-07-20, отступление от rev8 «пушит владелец» сознательное).
Mimo деплоит на следующий день.

## Что от тебя ждут (один проход, по rev8)

1. Ревью выполненного за цикл → вердикт: **зелёный** (можно пушить) или
   список блокеров.
2. Итоги цикла + раздача задач следующего цикла (открытые хвосты —
   в секции ниже).
3. Оформление — по твоему обычному каналу (follow-ups/ролевая ревизия;
   файлы за твоим авторством, single-writer правило в силе).

## Уже верифицировано второй моделью (лимит сюда не тратить)

| Задача | Коммиты | Верификатор |
|---|---|---|
| Scraper UUID cache (`97947ae`), Task A (`d680977`), Task C (`aea9b04`), Task B (`13b99fc`), cleanup A (`9e230e3`) | — | **ты сам** (follow-ups-2026-07-19/20) |
| Task 6 (`2f8d581`) | re-resolve stale UUID | **Codex** — принято, блокеров нет |
| Task 9 (`506b6a9`) | битый импорт shorts | **Codex** — принято |
| Task 8 (`af92e05` + `0b78575`) | `<pre><code>` + фенсы | **Codex** + **Kimi** — принято |

Содержание ревью: `codex-task6-review-2026-07-20.md`,
`codex-task8-task9-review-2026-07-20.md` (**untracked в дереве — не
потерять, коммит в чек-листе манифеста**), `kimi-task8-review-2026-07-20.md`.

## Зона, не покрытая второй парой глаз

- **Task D (`82a1251`, MiniMax)** — 10 хронических падений в 7 файлах.
  Критерий «suite зелёный» выполнен (715/715, прогон Kimi 14:10), но
  содержательной кросс-модельной ревью правок не было. Это единственный
  кодовый коммит цикла без второй пары глаз — рекомендуемый фокус твоего
  прохода (приёмка по чек-листу калибровки: моки по смыслу, скоуп,
  честность отчёта).
- Процессные ревизии rev6–8 и README-синки — твои/Mavis'ины доки,
  верификации по правилам не требуют.

## Открытые хвосты для раздачи следующим циклом

1. **Task 7** — заморожен, ждёт решения владельца о выносе
   `analyticsChannelId` из JSON настроек.
2. Гонка register в скрейпере (неблокирующая заметка Codex к Task 6) —
   сначала подтвердить контракт `POST /api/v1/monitoring/channels`
   (идемпотентный upsert / unique conflict / дубликаты).
3. План конвергенции иерархий (`docs/platform-convergence-table.md`) —
   ждёт утверждения владельца; открытые вопросы перечислены в конце дока
   (живость `routes/content|social`, судьба dead code).
4. Замечание к мокам Task C (admin-ветка в роут-тестах) — учесть при
   следующих правках роутов, не блокер.

## Состояние доски

`vitest run` — 69/69 файлов, 715/715 (прогоны Codex и Kimi совпали);
`tsc -p tsconfig.critical.json` — exit 0; 27 коммитов к пушу, разбор —
в `kimi-push-manifest-2026-07-20.md`.
