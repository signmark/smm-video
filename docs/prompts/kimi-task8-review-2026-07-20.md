# Kimi review: Task 8 (`af92e05` + `0b78575`) — 2026-07-20

Статус: **принято, блокирующих замечаний нет.** Вторая пара глаз по
конвейеру Codex → Kimi (запрошена в `codex-task8-task9-review-2026-07-20.md`).

Исполнитель: Claude (`af92e05`), Codex (`0b78575`, follow-up по находке ревью).
Верификатор: Kimi — код из «моей» зоны (`server/utils/telegram-html.ts`,
архитектура изоляции/восстановления из Task A, `d680977`).

## Что перепроверено

1. **Порядок конвейера сохранён.** Конвертация markdown-фенсов в `<pre>`
   стоит ДО `extractCodeBlocks`, восстановление — последним шагом.
   Инварианты Task A не нарушены: плейсхолдеры `\x00CB<n>\x00` не видят
   ни markdown-, ни HTML-конвертаций, экранирование содержимого однократное
   (`escapeCodeContent` — вынесенный общий хелпер, идентичен прежнему
   inline-коду).
2. **`<pre><code class="language-…">` распознаётся корректно:**
   - `<pre><code>x</code></pre>` → теги сохранены, содержимое экранировано;
   - language-класс (в двойных кавычках) сохраняется, прочие классы
     отбрасываются — как заявлено в commit message;
   - незакрытый `<code>` и хвостовой текст после `</code>` деградируют в
     экранированный текст внутри `<pre><code>` — текст не теряется;
   - обычный `<pre>x</pre>` идёт прежней веткой.
3. **Фенсы:** многострочный fence требует `\r?\n` после optional language —
   ` ```js``` ` без перенова больше не теряет текст (исходная находка
   Codex), а становится `<code>js</code>`; регрессионный тест на месте.
   Пустые `` ``` ``` `` вычищаются `dropEmptyTags`. Непарные backticks
   остаются литералом — приемлемая деградация.
4. **Взаимодействие с моими поздними правками:** пустые `<code></code>`
   от inline-ветки снимаются моим расширением `dropEmptyTags` (`9e230e3`);
   zero-width/BOM внутри фенсов не чистится (изоляция раньше) — осознанно,
   код не должен мутировать.
5. **Независимый прогон (мой, после `0b78575`):**
   `vitest run` — 69/69 файлов, 715/715 тестов, длительность ~5 c;
   `tsc -p tsconfig.critical.json` — exit 0. Совпадает с цифрами Codex.
6. `git show --stat`: в обоих коммитах только `telegram-html.ts` и его
   тест — скоуп чистый.

## Неблокирующие замечания

- `class='language-x'` в одинарных кавычках распознаётся как «прочий
  класс» и отбрасывается (паттерн только на двойные кавычки). Деградация
  безопасная, поведение задокументировано коммитом — оставить как есть,
  если редакторы такое не генерируют.
- Inline ` ```code``` ` — нестандартный markdown (GFM такое не
  конвертирует), но как сохранение текста против потери — правильный
  выбор; зафиксировано тестом.
- Прежний трейд-офф decode-first (`&#96;` → backtick → участвует в
  фенс-матчинге) остаётся в силе по DO NOT FIX, не регрессия.

## Итог

`af92e05` и `0b78575` можно включать в ближайший push. Мой шаг конвейера
по Task 8 закрыт. Собственные файлы Kimi в этом проходе: только этот
review-артефакт.
