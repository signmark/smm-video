# Review follow-ups — 2026-07-20 (Claude, ревью коммитов d680977 и 97947ae)

## Дополнение (цикл мониторинга, вечер 2026-07-20): приёмка Task C и статусы

### Task C (aea9b04, MiniMax) — ✅ ПРИНЯТ. Калибровка пройдена

Проверено по чек-листу калибровки:

1. **Полнота** — 7/7 тестов починены, дельта точно по промпту
   (9 файлов/17 тестов → 7/10), новый baseline корректен.
2. **Дисциплина скоупа** — образцовая: чужие WIP (Task B, Task A) явно
   не тронуты и названы в commit message; прод-код не изменён.
3. **Моки по смыслу, не для зелёного цвета** — проверено содержательно:
   стаб gemini-image с `success:false` не прячет тест, а *восстанавливает*
   его исходное намерение (тест называется «via FAL API» — теперь FAL-ветка
   реально исполняется); мок `rewriteContent` переставлен с неверного
   сервиса на реальный вызываемый (`aiService`) с правильной формой ответа;
   no-op `authenticateUser` — стандартная практика для роут-тестов.
4. **Честность отчёта** — полный vitest до/после, wall clock, root cause
   по каждому тесту. Лучший commit message цикла.

**Решение: MiniMax переведён в основной ростер.** Следующая задача —
Task 8 (`<pre><code>`), как и планировалось.

Замечание по мокам (не блокер, учесть в Task D): no-op `authenticateUser`
ставит `is_smm_admin: true` — все 5 роут-тестов теперь ходят админской
веткой. Если в роутах появится ветвление по админству, нужен отдельный
тест с `is_smm_admin: false`.

**Процессное нарушение (на будущее):** коммит `aea9b04` сделан ДО
кросс-модельной содержательной приёмки (Mavis принял сдачу той же модели).
Правило из roles-дока: сдача MiniMax → содержательная проверка Claude/Codex →
только потом коммит. В этот раз сдача оказалась чистой; правило существует
для тех раз, когда не окажется.

### Task 9 (HOTFIX, из таблицы конвергенции Кими, ПОДТВЕРЖДЁН пробой Claude)

**Файл:** `server/services/social/index.ts:126`.

`await import('./social-platforms/youtube-shorts-service')` — файл лежит в
`server/services/social/`, путь резолвится в несуществующий
`social/social-platforms/…`. Публикация YouTube Shorts через легаси-фасад
(`clips-publishing-router`, `publishing-routes`, `publish-scheduler:1640`)
падает в рантайме с module-not-found. Фикс — одна строка: `../social-platforms/…`.

**✅ ЗАКРЫТ:** `506b6a9` (Claude, по приказу владельца «закрыть все таски»;
кросс-модельная верификация — следующей сессией Codex/Mavis, применение
fallback-правила зафиксировано). Регрессионный тест резолвит ВСЕ
относительные импорты `services/social/` и сразу поймал второй битый
импорт: `telegram-proxy-service.ts` → несуществующий `shared/types`.
Вердикт dead code **доказан** (файл не может быть загружен никем);
удаление — по решению владельца, файл в KNOWN_DEAD-списке теста.
**Приёмка (исходная):** юнит-тест
на shorts-ветку фасада (или хотя бы smoke-импорт), полный vitest зелёный
(теперь это снова что-то значит — 701/701).

### Приёмка таблицы конвергенции (8167b72, Kimi) — ✅ ПРИНЯТА

Все 22 файла покрыты вердиктами, «кто зовёт» собран свежим grep, порядок
миграции и открытые вопросы на месте. Две ценные находки сверх задачи:
Task 9 (подтверждён, см. выше) и два кандидата в dead code
(`social/telegram-proxy-service.ts`, 613 строк, не вызывается никем;
`social-platforms/base-service.ts`, 27 строк) — на подтверждение владельцем,
затем отдельный промпт на удаление.

### Прочие статусы цикла

- **Task B (13b99fc)** и **cleanup Task A (9e230e3)** — закоммичены,
  соответствуют принятым мной WIP-диффам; цепочка «сдал → принял →
  закоммитил» зафиксирована в сообщениях. ✅
- **Task 6 (WIP в дереве)** — предварительный просмотр: на верном пути.
  `reresolveAnalyticsChannel` с guard'ом «тот же UUID = причина не в кеше»
  элегантнее моего ТЗ; re-resolve только для кешированных ID; исходные
  ошибки пробрасываются. Финальная приёмка — по сдаче.
  **Сдан: `2f8d581` (Kimi, 2026-07-20), 39/39 тестов аналитики + tsc чисто;
  содержательная верификация — Codex, до пуша.**
- **Task D (WIP в дереве)** — начат (environment-detector, logger).
- **ТЗ `kimi-convergence-table.md`** — вычитано: качественное (приёмка,
  out-of-scope, DO NOT FIX на месте). Одно замечание: имя файла выбивается
  из конвенции промптов (`codex-<суть>.md` / `<автор>-<срез>-дата.md`) —
  это промпт, а назван как срез. Не переименовывать сейчас (уже staged),
  учесть в следующих.

---

Оба коммита приняты, регрессий нет. Ниже — новые находки для оформления
в промпты. Прежние правила в силе: «подтверждено» перепроверять пробой,
`DO NOT FIX` не пересматривать.

## Вердикты по коммитам

- **d680977 (Task A, pre/code + hex)** — ✅ выполнен качественно. Изоляция
  через `\x00CB{i}\x00`-плейсхолдеры корректна, hex-сущности декодируются,
  суррогатные code points отклонены в обеих ветках (сверх ТЗ — правильно),
  10 новых тестов осмысленны. Task 1 из follow-ups-2026-07-19 закрыт.
- **97947ae (analytics channel UUID + удаление кнопок)** — ✅ принят.
  Сигнатуры использованы верно (`getAllMonitoredChannels({platform}, true)`
  существует, `ParseStatus.last_parsed_at` есть — переход с
  `found.last_parsed_at` корректен и точнее). Ветка
  `resolvedChannelCount === 0` — улучшение против старого поведения.
  Отдельный плюс: commit message честно отчитывается о полном vitest
  против baseline. Тесты 46/46.

## Новые задачи

### Task 6 (medium): протухший analyticsChannelId никогда не инвалидируется

**Файл:** `server/services/scraper-analytics.ts` (`resolveAnalyticsChannel`),
`server/services/analytics-service.ts` (`supplementFromScraper`).

`resolveAnalyticsChannel` делает `if (cachedId) return cachedId` — кеш
вечный. Если канал в скрейпере удалили/пересоздали (UUID сменился),
`getChannelAnalytics(staleId)` вернёт null/ошибку → `continue`, и аналитика
для кампании молча умирает навсегда: путь повторного lookup недостижим,
пока в settings лежит битый UUID.

**Что сделать:** при неуспехе `getChannelAnalytics`/`getChannelParseStatus`
по кешированному ID — один retry: сбросить `analyticsChannelId` (или
передать `cachedId: null`), пере-résolve через lookup→register, при успехе
персистнуть новый UUID. Защититься от зацикливания (не более одного
re-resolve за вызов).

**Приёмка:** юнит-тест в `scraper-analytics-resolve.test.ts`: кешированный
UUID даёт 404 → происходит lookup → возвращается свежий UUID и
персистится обратно.

### Task 7 (low-medium): lost-update в persistAnalyticsChannelId

**Файл:** `server/services/scraper-analytics.ts`.

`persistAnalyticsChannelId` делает GET→PATCH **всего**
`social_media_settings` fire-and-forget. Per-campaign очередь сериализует
только собственные записи; если пользователь в этот момент сохраняет
настройки кампании (а там живут токены), его изменения между GET и PATCH
будут затёрты. Окно маленькое, но ставка — токены.

**Что сделать (минимум):** сузить окно — перечитывать settings
непосредственно перед PATCH уже делается; добавить сравнение: если
прочитанные settings отличаются от тех, что были при resolve (по
updated_at кампании или deep-equal интересующей платформы), — перечитать
ещё раз и мержить только поле `analyticsChannelId`. Идеально — если
Directus-схема позволит вынести `analyticsChannelId` из JSON в отдельное
поле; это решает класс проблем (обсудить с владельцем, не делать молча).

### Task 8 — ✅ ЗАКРЫТ: `af92e05` (Claude, по приказу владельца; верификация — следующей сессией Codex/Mavis)

Сделано: `<pre><code class="language-…">` сохраняется нативно; бонусом
закрыт смежный edge — markdown-фенсы конвертируются в `<pre>` ДО изоляции
(раньше протекали как ``<code>…</code>`` с мусорными бэктиками — фенсы
не обрабатывались в `markdownToTelegramHtml` вовсе). 7 новых тестов,
полный набор 69 файлов / 714 тестов зелёный. Исходная формулировка ниже.

### ~~Task 8 (small): `<pre><code>…</code></pre>` рендерит внутренний тег литерально~~

**Файл:** `server/utils/telegram-html.ts`.

Стандартная связка редакторов `<pre><code>x</code></pre>`: pre-стэш
захватывает содержимое вместе с `<code>`-тегами и экранирует их →
пользователь видит литеральный текст `<code>x</code>`. Telegram
нативно поддерживает `<pre><code class="language-…">`.

**Что сделать:** в `restoreCodeBlocks` для kind='pre' распознавать
единственный внутренний `<code[^>]*>…</code>`, сохранять его тегом
(и `class="language-…"`, если есть), экранируя только содержимое.

**Приёмка:** тест: `<pre><code>x &lt; y</code></pre>` →
`<pre><code>x &lt; y</code></pre>`.

**Смежный edge (в эту же задачу, одной строкой в тесты):** markdown-код
(` ```…``` ` и `` `…` ``) конвертируется в `<code>`/plain-текст ПОСЛЕ
изоляции HTML-блоков, поэтому HTML внутри markdown-фенсов не защищён.
Зафиксировать текущее поведение тестом или перенести
`markdownToTelegramHtml` до `extractCodeBlocks` с защитой фенсов —
на усмотрение исполнителя, но осознанно.

## Обновления статусов (для README, правит оркестратор)

- `codex-analytics-channel-id-and-remove-button.md`: статус
  «в working tree» устарел — реализация закоммичена как `97947ae`.
- В очередь после деплоя: проверка обогащения `analyticsChannelId`
  по существующим кампаниям (уже записано в README, пункт остаётся).

## DO NOT FIX (без изменений)

Все пункты из `review-follow-ups-2026-07-19.md` в силе. Дополнение:
вечный кеш `analyticsChannelId` НЕ чинить наивным TTL — только
re-resolve по факту ошибки (Task 6), иначе вернём лишний lookup
на каждый заход в аналитику, ради устранения которого фича и делалась.
