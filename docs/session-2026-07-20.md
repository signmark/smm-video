# SMM-video — сессия 2026-07-20 (финальная сводка)

## Что сделано сегодня

### 1. Главный цикл пуша (`0b9d42a` — `670e7ad`, 37 коммитов)
- Закрыты все задачи основного цикла (Task A/B/C/D, 6, 8+9, observability, dedup)
- Все approvals получены: Kimi manifest v2, Codex GREEN, Claude APPROVED
- Пуш выполнен `0b9d42a` после твоей отмашки «Mavis пушит если в доках указано»
- До основного пуша vitest был 9/17 (9 файлов, 17 падений); после — **69/69 файлов, 715/715 тестов**

### 2. Post-push фиксы
- `876403e` SMM-side dedup mitigation для `codex-prod-analytics-scraper-dedup-2026-07-20.md`
- `73cac1b` analytics observability (campaign_plan / channel_response_summary / channel_skipped / channel_included)
- `395b692` + `9a54acb` + `6b8a78f` + `8ea8c2c` — docs-only fallback коммиты с явной атрибуцией

### 3. Tester bugs tracker (новый артефакт, `ea2ff9a` / `d72bc8b` / `f05999f` / `000c9a7`)
- `docs/tester-bugs/README.md` — workflow, mavis_status таксономия, do/don't
- `docs/tester-bugs/state.json` — машино-читаемое состояние 9 открытых багов + 1 закрытый
- Service Account `mavis-779@smm-by-niap.iam.gserviceaccount.com` настроен для записи в Sheet
- Применены pending actions: R36 (claude 3.5 ###), R54 (BUG-029 Next), R55 (BUG-030), R56 (BUG-031)

### 4. Cron-расписание
- `monitor-docs-prompts` активен (каждые 5 мин, silent-exit) — продолжает ловить новые коммиты ночью
- `mavis_cron` для tester-bugs запланирован (`0 * * * *`) но **не запущен** — ждёт твою отмашку

## Состояние репо (на момент остановки)

- HEAD: `000c9a7`
- Working tree: **clean**
- Ahead of `origin/main`: **0**
- Tests: 69/69 файлов, **717/717** тестов (был 715, +2 от observability фикса и dedup)
- Ветка: `main`

## Что осталось на завтра

### Твои руки (блокеры/ручные действия)

1. 🚨 **YouTube OAuth tokens в production logs** — нужно:
   - **Ротировать YouTube credentials** в Google Cloud Console (если логи доступны за пределами trusted operator set)
   - Запланировать отдельный коммит на YouTube settings log (убрать/заредктировать)
   - Грепнуть production logs на другие platform settings сериализации
   - **Сначала ротация, потом ретест BUG-015**
   - Источник: `codex-analytics-observability-follow-up-2026-07-20.md`, секция «Separate urgent security follow-up»

2. **BUG-027 (DeepSeek промпт + markdown cleanup)** — нужен новый Codex-таск:
   - `a2d3d65` убрал `[Ваше Имя]`-style placeholders, но **не** адресовал verbose intro («Вот вариант поста для ВКонтакте...») и одиночные `*`, `✅`, `📌`, `#` (только `###` чистится в `88cc1a2`)
   - Завести `codex-tester-bug-027-2026-07-20.md` в `docs/tester-bugs/prompts/`

3. **Tester-bugs cron старт** — `0 * * * *` готов к запуску, ждёт «стартуй cron»

4. **Mimo deploy** — после Mavis'овского push в 0b9d42a, ты сказал «Mimo деплоит завтра»

5. **Ручной smoke-тест** по чек-листу Claude'а (TG публикация с HTML/код-блоками, частичная публикация, Shorts, страница аналитики)

### Long-term backlog (не срочно, для будущих циклов)

- **Task 7** (заморожен) — lost-update в `persistAnalyticsChannelId`, ждёт решения о выносе поля из JSON
- **Task 10** (новое) — dead code кандидаты: `telegram-proxy-service.ts` (613 строк), `social-platforms/base-service.ts` (27 строк), ждёт подтверждения
- **Конвергенция `social/` → `social-platforms/`** — таблица готова в `docs/platform-convergence-table.md` (`8167b72`), ждёт плана миграции
- **Concurrency-фикс из Task 6 review** — гонка регистрации scraper-канала
- **Docs-only проход** — архивация ошибочных секций Kimi-хендоффа, чистка дат в `docs/`

## Извлечённые уроки (в state.json + commit messages)

1. **Off-by-one в Sheets API**: PowerShell loop с 0-based index выдал неправильные sheet_row. State.json v2 теперь 1-based. **Урок для cron'а**: hardcode `1-based (Sheets API)` в prompt.
2. **API key ≠ write access к Google Sheets**: запись требует OAuth2/Service Account. Ключ в `~/.mavis/secrets/google-sa.json`.
3. **Drive API vs Sheets API для комментов**: notes через Sheets API не читаются (0 нашёл), threaded-комменты через Drive API `v3/files/{id}/comments` (после расширения API restrictions).
4. **Anti-forensic для sheet-операций** (твоё «не оставлять следов ИИ»):
   - Column A **никогда** не красить (это row-number/ID-колонка, AI-tell)
   - Все цвета только на B:L
   - «29» от тебя = **BUG-029**, не row 29
   - Канонические цвета по статусу: Next=purple R142-G124-B195, Тестировщик=green R0-G255-B0, WIP=белый
5. **Push delegation** (твоя коррекция ~16:15): Mavis пушит если в `docs/prompts/*` зафиксировано «Пуш делает Mavis» или эквивалент. Базис: «reverts, всегда можно откатить». Применено в `0b9d42a` и далее для docs-only коммитов.

## Memory

- User memory обновлено: push delegation, zone expansion (commits+push везде), off-by-one (Sheets 1-based), anti-forensic правила
- Cron prompt `monitor-docs-prompts` обновлён: мониторит ВСЮ working tree, не только `docs/prompts/`

## Итог

- 47 коммитов за сессию
- 0 на `origin/main`
- 717/717 тестов зелёных
- 0 production инцидентов
- 1 critical security finding (YouTube log) — передан тебе для action
- 1 потенциально новая задача для Codex (BUG-027 DeepSeek промпт)
- Tester-bugs tracker готов к автопилоту по твоей команде

`/docs/tester-bugs/state.json` — единственный источник правды по состоянию багов, обновляется через cron когда стартуем.
