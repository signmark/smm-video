#!/bin/bash

# Скрипт для запуска всех тестов HTML-форматирования в Telegram
# Запуск: ./run-tests.sh

# Цвета для вывода
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}======================================================${NC}"
echo -e "${BLUE}📱 ТЕСТЫ HTML-ФОРМАТИРОВАНИЯ В TELEGRAM${NC}"
echo -e "${BLUE}======================================================${NC}"
echo ""

# Проверяем доступность сервера
echo -e "${YELLOW}Проверка доступности сервера...${NC}"
if curl -s http://localhost:3000 > /dev/null; then
  echo -e "${GREEN}✅ Сервер доступен!${NC}"
else
  echo -e "${RED}❌ Сервер недоступен! Убедитесь, что приложение запущено.${NC}"
  exit 1
fi
echo ""

# Запускаем тесты для текстового форматирования
echo -e "${YELLOW}Запуск тестов HTML-форматирования текста...${NC}"
node run-tests-telegram-html.js

# Пауза между тестами
echo ""
echo -e "${YELLOW}Пауза 5 секунд перед следующими тестами...${NC}"
sleep 5
echo ""

# Запускаем тесты для изображений с форматированными подписями
echo -e "${YELLOW}Запуск тестов отправки изображений с HTML-форматированием...${NC}"
node run-tests-telegram-images.js

echo ""
echo -e "${BLUE}======================================================${NC}"
echo -e "${GREEN}✅ ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ${NC}"
echo -e "${BLUE}======================================================${NC}"
