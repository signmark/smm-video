# Техническое задание: Исправление бага повторной публикации

## 🚨 Описание проблемы

**Критический баг:** Один пост публикуется 5-7 раз подряд в течение 30 секунд  
**Последствия:** Спам в социальных сетях, некорректная аналитика, возможные блокировки аккаунтов  
**Источник:** Планировщик `publish-scheduler.ts` неправильно проверяет статусы публикации  

## 📊 Доказательства проблемы

### N8N Workflow Logs:
```
Posting TG:
- 18:42:01 - Success (5526)
- 18:41:53 - Success (5525) 
- 18:41:43 - Success (5524)
- 18:41:43 - Success (5523)
- 18:41:41 - Success (5522)
- 18:41:36 - Success (5521)
- 18:41:34 - Success (5520)

Posting VK:
- 18:40:39 - Success (5519)
- 18:40:35 - Success (5518)
- 18:40:34 - Success (5517)
- 18:40:34 - Success (5516)
- 18:40:28 - Success (5515)
- 18:40:28 - Success (5514)
```

**Результат:** 13 дублированных публикаций за 1 минуту!

## 🔍 Анализ кода

### Проблемная логика в планировщике:

```typescript
// ПРОБЛЕМНЫЙ КОД (ДО ИСПРАВЛЕНИЯ):
if (platformData.status === 'pending') {
  log(`${platform}: статус "pending", ГОТОВ К НЕМЕДЛЕННОЙ ПУБЛИКАЦИИ`, 'scheduler');
  return true; // ПУБЛИКУЕМ МНОГОКРАТНО!
}
```

**Проблема:** Планировщик считает любую платформу со статусом `pending` готовой к публикации, **не проверяя** признаки уже состоявшейся публикации.

## ✅ Решение (РЕАЛИЗОВАНО 24.05.2025)

### Критические проверки добавлены в код:

```typescript
// РЕАЛИЗОВАННЫЙ КОД:
// КРИТИЧЕСКАЯ ЗАЩИТА #1: Проверяем признаки уже выполненной публикации
if (platformData.postUrl && platformData.postUrl.trim() !== '') {
  log(`  - ${platform}: УЖЕ ОПУБЛИКОВАН, найден postUrl: ${platformData.postUrl}, ПРОПУСКАЕМ`, 'scheduler');
  return false;
}

// КРИТИЧЕСКАЯ ЗАЩИТА #2: Проверяем timestamp публикации
if (platformData.publishedAt) {
  log(`  - ${platform}: УЖЕ ОПУБЛИКОВАН, найден publishedAt: ${platformData.publishedAt}, ПРОПУСКАЕМ`, 'scheduler');
  return false;
}

// ИСПРАВЛЕНИЕ: Для платформ в статусе 'pending', включаем только если нет признаков публикации
if (platformData.status === 'pending') {
  log(`  - ${platform}: статус "pending", ГОТОВ К НЕМЕДЛЕННОЙ ПУБЛИКАЦИИ`, 'scheduler');
  return true;
}
```

**Местоположение:** `server/services/publish-scheduler.ts`, строки 1503-1519

## 📁 Файлы для изменения

### Основной файл:
```
server/services/publish-scheduler.ts
├── Класс PublishScheduler
├── Метод publishContent()
└── Логика фильтрации платформ
```

## 🔧 Детальная реализация

### 1. Класс планировщика публикаций

```typescript
// server/services/publish-scheduler.ts
import axios from 'axios';
import { log } from '../utils/logger';

export class PublishScheduler {
  private isRunning = false;
  private intervalId: NodeJS.Timeout | null = null;
  private checkIntervalMs = 20000; // 20 секунд между проверками
  
  // Кэш токенов для избежания повторных авторизаций
  private adminTokenCache: string | null = null;
  private adminTokenTimestamp: number = 0;
  private tokenExpirationMs = 30 * 60 * 1000; // 30 минут

  /**
   * Запускает планировщик публикаций
   */
  start() {
    if (this.isRunning) {
      log('Планировщик публикаций уже запущен', 'scheduler');
      return;
    }

    log('Запуск планировщика публикаций', 'scheduler');
    this.isRunning = true;
    
    // Немедленно выполняем первую проверку
    this.checkScheduledContent();

    // Устанавливаем интервал для регулярных проверок
    this.intervalId = setInterval(() => {
      this.checkScheduledContent();
    }, this.checkIntervalMs);
  }

  /**
   * Останавливает планировщик публикаций
   */
  stop() {
    if (!this.isRunning || !this.intervalId) {
      log('Планировщик публикаций не запущен', 'scheduler');
      return;
    }

    log('Остановка планировщика публикаций', 'scheduler');
    clearInterval(this.intervalId);
    this.intervalId = null;
    this.isRunning = false;
  }

  /**
   * Основная логика проверки запланированного контента
   */
  async checkScheduledContent() {
    try {
      log('Проверка запланированных публикаций', 'scheduler');
      
      // Получаем системный токен для доступа к API
      const authToken = await this.getSystemToken();
      if (!authToken) {
        log('Не удалось получить токен для проверки контента', 'scheduler');
        return;
      }

      // Получаем контент для публикации
      const scheduledContent = await this.getScheduledContent(authToken);
      
      if (!scheduledContent || scheduledContent.length === 0) {
        log('Нет контента, готового к публикации по времени', 'scheduler');
        return;
      }

      log(`Найдено ${scheduledContent.length} элементов для публикации`, 'scheduler');

      // Публикуем каждый элемент контента
      for (const content of scheduledContent) {
        await this.publishContent(content, authToken);
      }
    } catch (error: any) {
      log(`Ошибка при проверке запланированного контента: ${error.message}`, 'scheduler');
    }
  }

  /**
   * КРИТИЧЕСКАЯ ФУНКЦИЯ: Публикация контента с защитой от дублей
   */
  async publishContent(content: any, authToken: string) {
    try {
      log(`[Публикация] Начало публикации контента ID: ${content.id}`, 'scheduler');
      
      if (!content.id || !content.campaignId) {
        log(`Контент с ID ${content.id} не содержит необходимой информации`, 'scheduler');
        return;
      }
      
      // ЖЕСТКАЯ проверка статуса ПЕРЕД публикацией
      if (content.status === 'published') {
        log(`БЛОКИРОВКА: Контент ${content.id} имеет глобальный статус "published"`, 'scheduler');
        return;
      }
      
      // Проверяем статус в социальных платформах
      if (!content.socialPlatforms || typeof content.socialPlatforms !== 'object') {
        log(`Контент ${content.id} не имеет настроек платформ`, 'scheduler');
        return;
      }

      // Парсим social_platforms если это строка
      let platforms = content.socialPlatforms;
      if (typeof platforms === 'string') {
        try {
          platforms = JSON.parse(platforms);
        } catch (e) {
          log(`Ошибка парсинга social_platforms для контента ${content.id}: ${e}`, 'scheduler');
          return;
        }
      }

      log(`Публикация контента ${content.id}: "${content.title}"`, 'scheduler');
      
      // КРИТИЧЕСКАЯ ЛОГИКА: Фильтруем платформы для публикации
      const platformsToPublish = Object.entries(platforms)
        .filter(([platform, platformData]: [string, any]) => {
          // ЗАЩИТА #1: Если платформа уже опубликована, пропускаем
          if (platformData.status === 'published') {
            log(`  - ${platform}: статус "published", ПРОПУСКАЕМ`, 'scheduler');
            return false;
          }
          
          // ЗАЩИТА #2: Если уже есть postUrl или publishedAt - НЕ публикуем повторно!
          if (platformData.postUrl || platformData.publishedAt) {
            log(`  - ${platform}: УЖЕ ОПУБЛИКОВАН (URL: ${platformData.postUrl ? 'есть' : 'нет'}, дата: ${platformData.publishedAt ? 'есть' : 'нет'}), ПРОПУСКАЕМ`, 'scheduler');
            return false;
          }
          
          // ЗАЩИТА #3: Только для платформ в статусе 'pending' БЕЗ признаков публикации
          if (platformData.status === 'pending') {
            log(`  - ${platform}: статус "pending", ГОТОВ К ПУБЛИКАЦИИ`, 'scheduler');
            return true;
          }
          
          // ЗАЩИТА #4: Проверяем время публикации для scheduled статуса
          if (platformData.status === 'scheduled' && platformData.scheduledAt) {
            const scheduledTime = new Date(platformData.scheduledAt);
            const currentTime = new Date();
            
            if (scheduledTime <= currentTime) {
              log(`  - ${platform}: время публикации наступило, ГОТОВ К ПУБЛИКАЦИИ`, 'scheduler');
              return true;
            } else {
              const diffMinutes = Math.floor((scheduledTime.getTime() - currentTime.getTime()) / 1000 / 60);
              log(`  - ${platform}: запланирован на ${scheduledTime.toISOString()}, еще ${diffMinutes} мин., ПРОПУСКАЕМ`, 'scheduler');
              return false;
            }
          }

          return false;
        });

      if (platformsToPublish.length === 0) {
        log(`Контент ${content.id} - нет платформ для публикации`, 'scheduler');
        return;
      }

      log(`Найдено ${platformsToPublish.length} платформ для публикации: ${platformsToPublish.map(([p]) => p).join(', ')}`, 'scheduler');

      // Публикуем в каждую выбранную платформу
      const publishResults = [];
      for (const [platform, platformData] of platformsToPublish) {
        const result = await this.publishToPlatform(content, platform, platformData, authToken);
        publishResults.push({ platform, result });
      }

      // Обновляем статус контента после публикации
      await this.updateContentStatus(content.id, publishResults, authToken);

    } catch (error: any) {
      log(`Ошибка при публикации контента ${content.id}: ${error.message}`, 'scheduler');
    }
  }

  /**
   * Публикация в конкретную платформу
   */
  async publishToPlatform(content: any, platform: string, platformData: any, authToken: string) {
    try {
      log(`Запуск публикации в ${platform} для контента ${content.id}`, 'scheduler');
      
      // Получаем URL webhook для платформы
      const webhookUrls = {
        telegram: process.env.N8N_TELEGRAM_WEBHOOK || 'https://n8n.nplanner.ru/webhook/publish-telegram',
        vk: process.env.N8N_VK_WEBHOOK || 'https://n8n.nplanner.ru/webhook/publish-vk',
        instagram: process.env.N8N_INSTAGRAM_WEBHOOK || 'https://n8n.nplanner.ru/webhook/publish-instagram'
      };

      const webhookUrl = webhookUrls[platform as keyof typeof webhookUrls];
      if (!webhookUrl) {
        throw new Error(`Webhook URL не найден для платформы ${platform}`);
      }

      // Формируем payload для N8N
      const payload = {
        contentId: content.id,
        campaignId: content.campaignId,
        platform,
        title: content.title,
        content: content.content,
        mediaLinks: content.mediaLinks || [],
        hashtags: content.hashtags || [],
        scheduledAt: platformData.scheduledAt || new Date().toISOString(),
        userId: content.userId
      };

      // ОТПРАВЛЯЕМ запрос в N8N
      const response = await axios.post(webhookUrl, payload, {
        headers: {
          'Content-Type': 'application/json'
        },
        timeout: 30000 // 30 секунд таймаут
      });

      if (response.status === 200 && response.data) {
        log(`✅ Публикация в ${platform} успешно запущена для контента ${content.id}`, 'scheduler');
        
        // СРАЗУ обновляем статус в Directus, чтобы предотвратить повторную публикацию
        await this.updatePlatformStatus(content.id, platform, 'publishing', {
          postUrl: response.data.postUrl,
          publishedAt: new Date().toISOString()
        }, authToken);
        
        return {
          success: true,
          postUrl: response.data.postUrl,
          publishedAt: new Date().toISOString()
        };
      } else {
        throw new Error(`Неожиданный ответ от N8N: ${response.status}`);
      }
    } catch (error: any) {
      log(`❌ Ошибка публикации в ${platform}: ${error.message}`, 'scheduler');
      
      // Обновляем статус ошибки
      await this.updatePlatformStatus(content.id, platform, 'failed', {
        error: error.message,
        failedAt: new Date().toISOString()
      }, authToken);
      
      return {
        success: false,
        error: error.message
      };
    }
  }

  /**
   * Получение токена для доступа к API
   */
  async getSystemToken(): Promise<string | null> {
    // Проверяем кэш токена
    const now = Date.now();
    if (this.adminTokenCache && 
        this.adminTokenTimestamp && 
        (now - this.adminTokenTimestamp < this.tokenExpirationMs)) {
      return this.adminTokenCache;
    }

    try {
      // Авторизуемся как администратор
      const email = process.env.DIRECTUS_ADMIN_EMAIL;
      const password = process.env.DIRECTUS_ADMIN_PASSWORD;
      
      if (!email || !password) {
        log('Отсутствуют учетные данные администратора в env переменных', 'scheduler');
        return null;
      }

      const directusUrl = process.env.DIRECTUS_URL || 'https://directus.nplanner.ru';
      const response = await axios.post(`${directusUrl}/auth/login`, {
        email,
        password
      });

      if (response.data?.data?.access_token) {
        // Сохраняем токен в кэше
        this.adminTokenCache = response.data.data.access_token;
        this.adminTokenTimestamp = now;
        
        log('Авторизация администратора успешна', 'scheduler');
        return this.adminTokenCache;
      }
    } catch (error: any) {
      log(`Ошибка авторизации администратора: ${error.message}`, 'scheduler');
    }

    return null;
  }

  /**
   * Получение запланированного контента
   */
  async getScheduledContent(authToken: string): Promise<any[]> {
    try {
      const directusUrl = process.env.DIRECTUS_URL || 'https://directus.nplanner.ru';
      const currentTime = new Date();

      // Получаем контент со статусом 'scheduled'
      const response = await axios.get(`${directusUrl}/items/campaign_content`, {
        headers: {
          'Authorization': `Bearer ${authToken}`,
          'Content-Type': 'application/json'
        },
        params: {
          filter: JSON.stringify({
            _or: [
              { status: { _eq: 'scheduled' } },
              { status: { _eq: 'draft' } }
            ],
            social_platforms: { _nnull: true }
          }),
          limit: 100
        }
      });

      if (!response.data?.data) {
        return [];
      }

      // Фильтруем контент, готовый к публикации
      const readyContent = response.data.data.filter((item: any) => {
        if (!item.social_platforms) return false;

        // Парсим platforms
        let platforms = item.social_platforms;
        if (typeof platforms === 'string') {
          try {
            platforms = JSON.parse(platforms);
          } catch (e) {
            return false;
          }
        }

        // Проверяем, есть ли платформы готовые к публикации
        return Object.values(platforms).some((platformData: any) => {
          // Пропускаем уже опубликованные
          if (platformData.status === 'published' || 
              platformData.postUrl || 
              platformData.publishedAt) {
            return false;
          }

          // Готовы к публикации: pending или scheduled с наступившим временем
          if (platformData.status === 'pending') {
            return true;
          }

          if (platformData.status === 'scheduled' && platformData.scheduledAt) {
            return new Date(platformData.scheduledAt) <= currentTime;
          }

          return false;
        });
      });

      log(`Получено ${response.data.data.length} элементов контента, из них готовы к публикации: ${readyContent.length}`, 'scheduler');
      return readyContent;
    } catch (error: any) {
      log(`Ошибка получения запланированного контента: ${error.message}`, 'scheduler');
      return [];
    }
  }

  /**
   * Обновление статуса платформы в Directus
   */
  async updatePlatformStatus(contentId: string, platform: string, status: string, additionalData: any = {}, authToken: string) {
    try {
      const directusUrl = process.env.DIRECTUS_URL || 'https://directus.nplanner.ru';
      
      // Получаем текущие данные контента
      const getResponse = await axios.get(`${directusUrl}/items/campaign_content/${contentId}`, {
        headers: {
          'Authorization': `Bearer ${authToken}`,
          'Content-Type': 'application/json'
        }
      });

      if (!getResponse.data?.data) {
        log(`Контент ${contentId} не найден при обновлении статуса`, 'scheduler');
        return;
      }

      let platforms = getResponse.data.data.social_platforms;
      if (typeof platforms === 'string') {
        platforms = JSON.parse(platforms);
      }

      // Обновляем статус платформы
      if (!platforms[platform]) {
        platforms[platform] = {};
      }

      platforms[platform] = {
        ...platforms[platform],
        status,
        platform,
        ...additionalData
      };

      // Сохраняем обновленные данные
      await axios.patch(`${directusUrl}/items/campaign_content/${contentId}`, {
        social_platforms: platforms
      }, {
        headers: {
          'Authorization': `Bearer ${authToken}`,
          'Content-Type': 'application/json'
        }
      });

      log(`Статус платформы ${platform} для контента ${contentId} обновлен на "${status}"`, 'scheduler');
    } catch (error: any) {
      log(`Ошибка обновления статуса платформы ${platform}: ${error.message}`, 'scheduler');
    }
  }

  /**
   * Обновление общего статуса контента
   */
  async updateContentStatus(contentId: string, publishResults: any[], authToken: string) {
    try {
      const successCount = publishResults.filter(r => r.result.success).length;
      const totalCount = publishResults.length;

      let newStatus = 'draft';
      if (successCount === totalCount) {
        newStatus = 'published';
      } else if (successCount > 0) {
        newStatus = 'partially_published';
      }

      const directusUrl = process.env.DIRECTUS_URL || 'https://directus.nplanner.ru';
      await axios.patch(`${directusUrl}/items/campaign_content/${contentId}`, {
        status: newStatus,
        published_at: successCount > 0 ? new Date().toISOString() : null
      }, {
        headers: {
          'Authorization': `Bearer ${authToken}`,
          'Content-Type': 'application/json'
        }
      });

      log(`Статус контента ${contentId} обновлен на "${newStatus}" (${successCount}/${totalCount} платформ)`, 'scheduler');
    } catch (error: any) {
      log(`Ошибка обновления статуса контента ${contentId}: ${error.message}`, 'scheduler');
    }
  }
}

// Создаем и экспортируем экземпляр планировщика
export const publishScheduler = new PublishScheduler();
```

### 2. Функция логирования

```typescript
// server/utils/logger.ts
export function log(message: string, component: string = 'system') {
  const timestamp = new Date().toLocaleTimeString('ru-RU');
  console.log(`${timestamp} [${component}] ${message}`);
}
```

### 3. Запуск планировщика в основном приложении

```typescript
// server/index.ts или server/app.ts
import { publishScheduler } from './services/publish-scheduler';

// Запускаем планировщик при старте приложения
publishScheduler.start();

// Останавливаем планировщик при завершении
process.on('SIGINT', () => {
  publishScheduler.stop();
  process.exit(0);
});

process.on('SIGTERM', () => {
  publishScheduler.stop();
  process.exit(0);
});
```

## 🔍 Логирование для отладки

### Критические сообщения лога:
```typescript
// Защита от повторных публикаций
log(`БЛОКИРОВКА: Контент ${contentId} уже опубликован`, 'scheduler');
log(`${platform}: УЖЕ ОПУБЛИКОВАН, ПРОПУСКАЕМ`, 'scheduler');

// Успешная публикация
log(`✅ Публикация в ${platform} успешно запущена`, 'scheduler');

// Статистика обработки
log(`Найдено ${totalCount} элементов, готовы к публикации: ${readyCount}`, 'scheduler');
```

## ✅ Критерии успеха

### Функциональные:
- [ ] Каждый пост публикуется строго **один раз**
- [ ] N8N workflows не запускаются повторно для опубликованного контента
- [ ] Логи показывают "УЖЕ ОПУБЛИКОВАН" для повторных попыток
- [ ] Планировщик работает каждые 20 секунд без дублей

### Технические:
- [ ] Статусы платформ корректно обновляются в Directus
- [ ] postUrl и publishedAt сохраняются при публикации
- [ ] Нет ошибок в логах сервера
- [ ] Защита работает для всех платформ (TG, VK, IG)

### N8N Мониторинг:
- [ ] В N8N логах НЕТ дублированных workflow executions
- [ ] Каждый workflow запускается только один раз на пост
- [ ] Временные интервалы между публикациями корректные

## 🚨 Проверка исправления

### До внедрения:
```
N8N Logs:
18:41:34 - Posting TG - Success (5520)
18:41:36 - Posting TG - Success (5521) ❌ ДУБЛЬ
18:41:41 - Posting TG - Success (5522) ❌ ДУБЛЬ
18:41:43 - Posting TG - Success (5523) ❌ ДУБЛЬ
```

### После внедрения:
```
N8N Logs:
18:41:34 - Posting TG - Success (5520) ✅ ЕДИНСТВЕННАЯ ПУБЛИКАЦИЯ

Server Logs:
[scheduler] telegram: УЖЕ ОПУБЛИКОВАН (URL: есть), ПРОПУСКАЕМ
[scheduler] БЛОКИРОВКА: Контент уже опубликован
```

## 🔧 Переменные окружения

```env
# N8N Webhooks
N8N_TELEGRAM_WEBHOOK=https://n8n.nplanner.ru/webhook/publish-telegram
N8N_VK_WEBHOOK=https://n8n.nplanner.ru/webhook/publish-vk
N8N_INSTAGRAM_WEBHOOK=https://n8n.nplanner.ru/webhook/publish-instagram

# Directus
DIRECTUS_URL=https://directus.nplanner.ru
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=secure_password
```

---

**Статус:** Критическое исправление реализовано ✅  
**Защита от дублей:** Активна ✅  
**Тестирование:** Готово к проверке ✅  
**Мониторинг:** N8N логи должны показать отсутствие дублей ✅